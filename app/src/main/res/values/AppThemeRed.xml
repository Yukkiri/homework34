<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppThemeRed" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/redText</item>
        <item name="colorButtonNormal">@color/redButton</item>
        <item name="colorPrimary">@color/redPrimary</item>
        <item name="colorPrimaryDark">@color/redPrimary</item>
        <item name="colorAccent">@color/redAccent</item>
        <item name="android:background">@color/redPrimary</item>
    </style>

</resources>